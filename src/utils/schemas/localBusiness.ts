/**
 * Schema JSON-LD per LocalBusiness (MUV Fitness)
 * ⚠️ DEPRECATO: Usa getLocalBusinessSchemaData() da src/config/businessData.ts
 * Questo file è mantenuto per retrocompatibilità
 */

import { getLocalBusinessSchemaData } from '@/config/businessData';

// Esporta lo schema generato dinamicamente dalla fonte di verità
export const localBusinessSchema = getLocalBusinessSchemaData();
